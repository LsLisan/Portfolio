# MD. Lisan Islam Portfolio

A React + Vite portfolio built for GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The GitHub Actions workflow in `.github/workflows/deploy.yml` publishes the `dist` folder whenever `main` is updated.
